{% extends 'base.html.twig' %}

{% block title %}Mot de passe oublié - SkillPath Premium{% endblock %}

{% block stylesheets %}
<style>
    .mesh-background {
        background-color: #ffffff;
        background-image: 
            radial-gradient(at 0% 0%, rgba(30, 136, 229, 0.1) 0px, transparent 50%),
            radial-gradient(at 100% 0%, rgba(67, 160, 71, 0.1) 0px, transparent 50%),
            radial-gradient(at 50% 50%, rgba(251, 192, 45, 0.05) 0px, transparent 50%),
            radial-gradient(at 100% 100%, rgba(30, 136, 229, 0.1) 0px, transparent 50%);
    }
    .dark .mesh-background {
        background-color: #020617;
        background-image: 
            radial-gradient(at 0% 0%, rgba(30, 136, 229, 0.05) 0px, transparent 50%),
            radial-gradient(at 100% 0%, rgba(67, 160, 71, 0.05) 0px, transparent 50%),
            radial-gradient(at 50% 50%, rgba(251, 192, 45, 0.02) 0px, transparent 50%),
            radial-gradient(at 100% 100%, rgba(30, 136, 229, 0.05) 0px, transparent 50%);
    }
    .glass-login {
        background: rgba(255, 255, 255, 0.7);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border: 1px solid rgba(255, 255, 255, 0.4);
    }
    .dark .glass-login {
        background: rgba(15, 23, 42, 0.7);
        border: 1px solid rgba(255, 255, 255, 0.05);
    }
    .vibrant-shimmer {
        background: linear-gradient(135deg, #1E88E5 0%, #43A047 100%);
        box-shadow: 0 10px 40px rgba(30, 136, 229, 0.3);
    }
    /* Autofill Fix */
    input:-webkit-autofill,
    input:-webkit-autofill:hover, 
    input:-webkit-autofill:focus, 
    input:-webkit-autofill:active{
        -webkit-box-shadow: 0 0 0 30px white inset !important;
        -webkit-text-fill-color: #0f172a !important;
        transition: background-color 5000s ease-in-out 0s;
    }
    .dark input:-webkit-autofill,
    .dark input:-webkit-autofill:hover, 
    .dark input:-webkit-autofill:focus, 
    .dark input:-webkit-autofill:active{
        -webkit-box-shadow: 0 0 0 30px #020617 inset !important;
        -webkit-text-fill-color: white !important;
        transition: background-color 5000s ease-in-out 0s;
    }
</style>
{% endblock %}

{% block body %}
<div class="min-h-screen mesh-background flex items-center justify-center p-6 relative overflow-hidden">
    <!-- Abstract shapes -->
    <div class="absolute -top-24 -left-24 w-96 h-96 bg-blue-400/10 rounded-full blur-3xl animate-pulse"></div>
    <div class="absolute -bottom-24 -right-24 w-96 h-96 bg-green-400/10 rounded-full blur-3xl animate-pulse" style="animation-delay: 2s"></div>
    
    <div class="max-w-4xl w-full relative z-10 animate-in fade-in zoom-in duration-700">
        <div class="glass-login p-10 lg:p-14 rounded-[3.5rem] shadow-2xl relative overflow-hidden flex flex-col md:flex-row gap-12 items-center">
            
            <!-- Left Side: Visual/Context -->
            <div class="md:w-1/2 text-center md:text-left">
                <div class="flex items-center gap-3 mb-10 group cursor-pointer justify-center md:justify-start">
                    <div class="w-10 h-10 vibrant-shimmer rounded-xl flex items-center justify-center shadow-lg shadow-blue-500/20">
                        <img src="{{ asset('images/logo.png') }}" alt="Logo" class="w-6 h-6 invert brightness-0">
                    </div>
                    <span class="text-xl font-black tracking-tight text-slate-900 dark:text-white">SkillPath</span>
                </div>
                
                <h2 class="text-4xl font-black text-slate-900 dark:text-white mb-6 leading-tight">Besoin d'un <br><span class="text-transparent bg-clip-text vibrant-shimmer">coup de main ?</span></h2>
                <p class="text-slate-500 dark:text-slate-400 font-medium leading-relaxed">
                    Pas de panique ! Entrez votre adresse email et nous vous enverrons un code de réinitialisation sécurisé pour votre compte.
                </p>
            </div>

            <!-- Right Side: Action -->
            <div class="md:w-1/2 w-full">
                <a href="{{ path('app_user_login') }}" class="inline-flex items-center text-[10px] font-black uppercase tracking-[0.2em] text-blue-600 dark:text-blue-400 hover:text-blue-700 dark:hover:text-blue-300 mb-8 gap-2 group">
                    <svg class="w-4 h-4 group-hover:-translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/></svg>
                    Retour à la connexion
                </a>

                {% for message in app.flashes('error') %}
                    <div class="bg-red-50 text-red-600 px-6 py-4 rounded-2xl mb-6 text-sm font-bold border border-red-100 flex items-center gap-3">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/></svg>
                        {{ message }}
                    </div>
                {% endfor %}

                <form action="{{ path('app_user_forgot_password') }}" method="POST" class="space-y-6">
                    <div class="space-y-2">
                        <label class="text-xs font-black uppercase tracking-widest text-slate-400 ml-1">E-mail de récupération</label>
                        <div class="relative group">
                            <input type="email" name="email" placeholder="votre.email@exemple.com" required
                                   class="w-full bg-white/50 dark:bg-slate-900/50 border border-slate-200 dark:border-slate-800 rounded-2xl py-4 px-6 focus:outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-500/5 transition-all outline-none dark:text-white placeholder-slate-300 dark:placeholder-slate-600">
                            <span class="absolute right-5 top-4 text-slate-300 group-focus-within:text-blue-500 transition-colors">
                                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 12a4 4 0 10-8 0 4 4 0 008 0zm0 0v1.5a2.5 2.5 0 005 0V12a9 9 0 10-9 9m4.5-1.206a8.959 8.959 0 01-4.5 1.206"/></svg>
                            </span>
                        </div>
                    </div>

                    <button type="submit" class="w-full vibrant-shimmer text-white font-black py-4 rounded-2xl transform active:scale-95 transition-all shadow-xl">
                        Envoyer le code
                    </button>
                    
                    <p class="text-center text-[10px] text-slate-400 font-bold uppercase tracking-widest">
                         Vérifiez également vos messages indésirables (Spam)
                    </p>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}
